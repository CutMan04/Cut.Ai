{
  "name": "wcmb-cutai",
  "version": "1.0.0",
  "description": "WCMB Cut.AI project with Vercel Speed Insights",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for static site'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.1.0"
  }
}
